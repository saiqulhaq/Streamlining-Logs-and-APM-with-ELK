# input {
  # beats {
    # port => 5044
  # }

  # tcp {
    # port => 50000
  # }
# }

input {
  file {
    path => "/var/log/rails/*.log"
    start_position => "beginning"
  }
}

filter {
  # Add any filters you need to process your logs
}

output {
  elasticsearch {
    index => "rails-logs-%{+YYYY.MM.dd}"
    hosts => "elasticsearch:9200"
    user => "logstash_internal"
    password => "${LOGSTASH_INTERNAL_PASSWORD}"
  }
}
